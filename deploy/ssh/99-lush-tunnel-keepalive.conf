# Detect half-open reverse SSH tunnels from the VPS side as well as the client side.
# This prevents a stale sshd listener from keeping a GPU port present but unusable.
ClientAliveInterval 15
ClientAliveCountMax 3
TCPKeepAlive yes
